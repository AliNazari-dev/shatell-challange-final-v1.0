export type Advantage = {
  id: number;
  title: string;
  icon: string;
  description: string;
};

export type Comment = {
  userImage: string;
  userName: string;
  timestamp: string;
  commentText: string;
};
